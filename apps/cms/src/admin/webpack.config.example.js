'use strict';

/* eslint-disable no-unused-vars */
module.exports = (config, webpack) => {
  // Note: we provide webpack above so you should not `require` it
  // Perform customizations to webpack eslint-config-custom
  // Important: return the modified eslint-config-custom
  return config;
};
